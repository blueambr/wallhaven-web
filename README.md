# Wallpapers lite

## Prerequisites

- `pn install`

## Dev

- `pn dev`

## Build

- `pn build`

## Preview

- `pn build`
- `pn preview`

## Config

- Edit `svelte.config.js`, if another adapter is needed (default: Cloudflare)
- Edit `src/config.ts`
- Edit `src/data.ts`
